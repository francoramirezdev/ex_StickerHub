services:
  db:
    image: mysql:8
    container_name: stickerhub_db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: stickerhub
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql


volumes:
  mysql_data:
